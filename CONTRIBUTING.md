# NOMAD-DnA 협업 규칙

> 이슈 기반 작업과 PR 리뷰를 위한 공통 운영 기준입니다.  
> 저장소별 CI와 보호 설정은 운영 안내와 실제 GitHub 설정을 함께 확인합니다.

## 1. 브랜치 운영

기본 구조는 **main + 짧게 유지하는 작업 브랜치**입니다. 초기에는 develop 브랜치를 따로 두지 않고, 릴리스 관리가 복잡해지면 추가 여부를 논의합니다.

| 브랜치 | 용도 | 이름 예시 |
| --- | --- | --- |
| main | 검증된 기준 코드·문서 | `main` |
| feat/* | 기능 추가 | `feat/12-sensor-parser` |
| fix/* | 오류 수정 | `fix/18-timeout-handling` |
| docs/* | 문서 작성·수정 | `docs/git-workflow` |
| refactor/* | 동작 유지, 구조 개선 | `refactor/24-driver-interface` |
| test/* | 테스트 추가·개선 | `test/27-parser-cases` |
| chore/* | 설정·의존성·유지보수 | `chore/30-build-config` |

숫자는 **실제로 존재하는 이슈 번호**입니다. 위 번호는 설명용 예시이며, 이슈가 없으면 번호를 생략합니다. 영문 소문자와 하이픈을 사용합니다.

- main에서 직접 작업하지 않고 최신 main에서 브랜치를 만듭니다.
- 기본은 작업 담당자별 브랜치입니다. 같은 브랜치를 여럿이 수정해야 한다면 먼저 역할을 맞춥니다.
- 브랜치를 오래 방치하지 말고 작은 단위로 PR을 올립니다.
- 병합된 브랜치를 다음 작업에 재사용하지 않습니다. 최신 main에서 새로 만듭니다.

## 2. 이슈로 작업 범위 정하기

기능 추가·버그 수정·설계 변경은 작업 전에 이슈를 작성하고 담당자를 정합니다. 단순 오탈자처럼 작은 변경은 PR 설명만으로 진행할 수 있습니다.

**이슈에 포함할 내용**

- 배경: 어떤 문제를 해결하는가?
- 목표와 범위: 무엇을 하고, 무엇은 이번에 하지 않는가?
- 완료 조건: 어떤 결과·테스트로 완료를 판단하는가?
- 담당자와 의존성: 누구의 작업이며, 먼저 필요한 작업은 무엇인가?
- 버그라면 재현 절차, 기대 동작, 실제 동작, 실행 환경과 로그

예시:

```markdown
## 배경
센서 데이터가 비어 있을 때 파서가 예외로 종료됩니다.

## 작업 범위
- 빈 입력 처리
- 오류 반환 방식 명시
- 정상 입력·빈 입력 테스트 추가

## 완료 조건
- 기존 정상 입력 동작 유지
- 빈 입력 테스트 통과
- 호출부에서 오류 상태 확인 가능
```

설계·인터페이스 변경은 구현 전에 이슈에서 합의하고, 결론을 남깁니다. 채팅에서 결정했더라도 다른 팀원이 찾을 수 있도록 이슈나 문서에 요약합니다.

## 3. 커밋 작성

형식은 `type: 변경 내용`을 기본으로 하고, 필요하면 `type(scope): 변경 내용`을 사용합니다. 본문 설명은 한국어로 작성해도 됩니다.

| type | 사용 기준 |
| --- | --- |
| feat | 새 기능 |
| fix | 버그 수정 |
| docs | 문서 |
| refactor | 기능 변화 없는 코드 구조 개선 |
| test | 테스트 |
| chore | 빌드 설정·의존성·기타 유지보수 |

예시: `fix(parser): 빈 센서 입력 처리`, `docs: 개발 환경 설정 방법 추가`

- 하나의 커밋에는 설명 가능한 한 가지 변경을 담습니다.
- `수정`, `최종`, `asdf`처럼 목적을 알 수 없는 메시지를 피합니다.
- 동작 변경과 대규모 자동 포맷 변경은 가능하면 분리합니다.
- 커밋 전 `git diff`와 `git diff --staged`로 실제 변경을 확인합니다.
- 실패 중인 작업을 중간 공유할 수는 있지만, Draft PR과 설명으로 상태를 알리고 준비 전에는 병합하지 않습니다.

## 4. PR 작성과 리뷰

작업 브랜치를 push한 뒤 **base: main ← compare: 작업 브랜치**로 PR을 엽니다. 개발 중 조기 피드백이 필요하면 Draft PR을 사용합니다.

**제목:** 커밋과 같은 형식으로 핵심 변경을 요약합니다.

**본문 예시:** 아래 이슈 번호와 테스트 항목은 실제 내용으로 바꿉니다.

```markdown
## 변경 목적
어떤 문제를 해결했는지 설명합니다.

## 주요 변경
- 변경한 기능·파일·인터페이스
- 영향받는 모듈

## 검증
- 실행한 명령 또는 시험 절차:
- 환경 / 보드 / 툴체인 버전:
- 결과:
- 미검증 항목과 이유:

## 관련 이슈
Closes #실제이슈번호

## 리뷰 요청
특히 확인받고 싶은 부분, 제약사항, 위험 요소
```

`Closes #번호`는 해당 이슈를 실제로 해결하는 PR에만 씁니다. 기본 브랜치로 병합되면 연결된 이슈가 자동 종료될 수 있습니다. 참고만 할 때는 `Related to #번호`로 연결합니다.

### 작성자

- PR을 열기 전 Files changed에서 불필요한 파일과 민감정보가 없는지 확인합니다.
- 프로젝트의 빌드·테스트를 수행하고 결과를 기록합니다. 실행하지 못한 테스트를 통과했다고 쓰지 않습니다.
- 리뷰어를 지정하고, 피드백 수정 후 같은 브랜치에 추가 commit·push합니다. 기존 PR이 갱신됩니다.
- 검증 결과가 바뀌면 PR 본문도 갱신합니다.

### 리뷰어

- 요구사항 충족, 오류·예외 처리, 인터페이스 영향, 테스트와 재현성을 확인합니다.
- 필수 수정과 선택 제안을 구분하고 이유를 설명합니다.
- 질문만 있으면 Comment, 필수 수정이 남으면 Request changes, 확인을 마치면 Approve를 사용합니다.
- 승인 후 변경이 추가되면 필요한 부분을 다시 확인합니다.

## 5. 병합과 작업 종료

**기본 운영 기준:** 작성자 외 쓰기 권한이 있는 팀원 1명 이상 승인 → 필수 수정과 리뷰 대화 해결 → 설정된 CI 통과 → 일반 Merge commit 병합. 추가 변경 후에는 필요한 재승인을 받습니다. 이 .github 저장소의 문서 검사는 docs-check이며, 다른 프로젝트의 CI는 별도로 구축합니다.

- 기본 병합 담당자는 작성자입니다. 부재 시 리뷰어와 담당자를 정하고, 승인·테스트·충돌 상태를 확인한 뒤 병합합니다.
- **branch → PR → review → merge** 순서로 진행합니다. 리뷰 전 직접 main에 push하거나 PR을 스스로 승인하지 않습니다.
- GitHub에서 **Create a merge commit → Merge pull request → Confirm merge**로 병합합니다. Squash and merge와 Rebase and merge는 사용하지 않습니다.
- 일반 Merge commit은 작업 브랜치의 개별 커밋을 유지하고 병합 커밋을 추가합니다. 커밋을 하나로 압축하거나 재작성하지 않으므로 작업 이력과 분기·병합 관계를 확인할 수 있습니다.
- 최종 병합 제목도 `type: 변경 내용` 형식으로 정리합니다.
- 병합 후 원격 작업 브랜치를 삭제하고, 로컬 main을 갱신합니다.
- 이슈의 완료 조건과 관련 문서를 확인하고 다음 작업은 새 브랜치에서 시작합니다.
- 긴급 수정도 가능한 한 작은 PR로 진행합니다. 예외적인 처리는 사유와 사후 검증을 기록합니다.

## 6. 파일·보안·재현성

- 비밀번호, API 토큰, SSH 개인키, 실제 인증정보가 든 `.env`는 커밋하지 않습니다.
- 공개 저장소이므로 개인정보, 비공개 설계 자료, 고객·기관 자료의 공개 가능 여부를 확인합니다.
- `.env.example`에는 실제 값이 아닌 예시 값만 넣습니다.
- 빌드 산출물·캐시·개인 IDE 설정은 프로젝트 `.gitignore`로 관리합니다.
- 대용량 로그·영상·데이터셋·바이너리는 팀이 정한 별도 저장소나 Git LFS 사용 여부를 먼저 협의합니다.
- 펌웨어·임베디드 변경에는 보드/장치, 툴체인, 펌웨어 버전, 실행 명령, 시험 조건을 남깁니다.
- 하드웨어 시험을 못 했다면 시뮬레이션·단위 테스트 결과와 실제 장치 미검증 상태를 구분합니다.
- 비밀정보가 올라갔다면 **먼저 해당 키를 폐기·재발급**하고 관리자에게 알립니다. 파일을 지우는 커밋만으로 과거 이력에서 사라지지 않습니다.

## 7. 프로젝트별 규칙

각 프로젝트 README에 설치·실행·빌드·테스트 절차를 두고, 공통 규칙과 다른 사항은 해당 저장소 CONTRIBUTING에 명시합니다. 규칙을 바꿀 때도 문서 PR로 이유와 적용 범위를 함께 검토합니다.

명령어와 충돌 해결 방법은 [Git 실전 가이드](https://github.com/NOMAD-DnA/.github/blob/main/docs/GIT_GUIDE.md)를 참고합니다.

## 공식 참고 자료

- [GitHub Flow](https://docs.github.com/en/get-started/using-github/github-flow)
- [GitHub의 병합 방식](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/about-merge-methods-on-github)
